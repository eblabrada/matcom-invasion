#include "title.h"

#include <ncurses.h>
#include <stdio.h>

void print_title_screen() {
    clear();
    for (int x = 0, i = 0; i < 3; x++, i++) {
        mvprintw(x, 0, "................................................................................");
    }

    mvprintw(3, 0, "...................  Keys                            ...........................");
    mvprintw(4, 0, "...................                                  ...........................");
    mvprintw(5, 0, "...................  Space : Start New Game / Shoot  ...........................");
    mvprintw(6, 0, "...................      Q : Quit / End Game         ...........................");
    mvprintw(7, 0, "...................      A : Move Left               ...........................");
    mvprintw(8, 0, "...................      D : Move Right              ...........................");
  
    for (int x = 9, i = 0; i < 3; x++, i++) {
        mvprintw(x, 0, "................................................................................");
    }

    refresh();
}